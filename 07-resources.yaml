kind: Pod
apiVersion: v1
metadata:
  name: resource
spec:
  containers:
    - name: nginx
      image: nginx
      resources:
       # soft limit
       requests:
        memory: "50Mi"  
        cpu: "100m"
       # hard limit
      limits: 
        memory: "100Mi"
        cpu: "150m"